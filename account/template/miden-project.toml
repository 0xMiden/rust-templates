[package]
name = "{{crate_name}}"
version = "0.1.0"

[lib]
kind = "account-component"
namespace = "miden:{{crate_name | replace: "_", "-" }}/miden-{{crate_name | replace: "_", "-" }}@0.1.0"

[dependencies]
miden-core = "=0.22.3"
miden-protocol = "=0.14.0"

[package.metadata.miden]
supported-types = ["RegularAccountUpdatableCode"]
